/**
Abstraction layer around POI/HSSF Excel workbook I/O library. As the POI/HSSF library is a memory hog and has a fairly ugly API, we recommend using the JExcelAPI library instead, which also has an abstraction layer in {@link edu.harvard.med.screensaver.io.workbook2}.
*/
package edu.harvard.med.screensaver.io.workbook;
