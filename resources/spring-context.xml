<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN 2.0//EN"
    "http://www.springframework.org/dtd/spring-beans-2.0.dtd">

<!-- A top-level, composite Spring context configuration file, intended for use with the Screensaver web 
 application, or any derived command-line application that requires the same configuration as the web 
 application, including database access.  Allows a full Spring configuration to be referenced via a single 
 file.  Mutually exclusive with spring-context-sans-db.xml, which is intended for command-line applications 
 that do not need database access (and that must be runnable when a database cannot be accessed). -->

<beans>

  <!-- Enable AspectJ annotation support 
  -->
  <bean
    class="org.springframework.aop.aspectj.annotation.AnnotationAwareAspectJAutoProxyCreator">
    <property name="includePatterns">
      <list>
        <value>.*Aspect$</value>
      </list>
    </property>
  </bean>
  
  <import resource="spring-context-persistence.xml"/>
  <import resource="spring-context-services.xml"/>
  <import resource="spring-context-ui.xml"/>
  <import resource="spring-context-logging.xml"/>
  
</beans>
