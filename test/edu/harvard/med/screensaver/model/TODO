EntityPersistenceTest:

- test bidirectionality of relationships over persist

HibernateBeansTest:

- test getId() is public and setId() is private
- test isVersioned
- test hbn properties have non-hbn equivalent public getters
